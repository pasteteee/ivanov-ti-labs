export type TaskLanguage = "en" | "ru";

export interface Lab1Task {
  id: string;
  title: string;
  formulation: string;
  inputHint: string;
  language: TaskLanguage;
}

export const lab1Config = {
  tasks: [
    {
      id: "grille",
      title: "Задание 1",
      formulation:
        "Метод поворачивающейся решётки 4×4: исходный текст записывается через отверстия решётки, решётка поворачивается на 90° по мере заполнения. В пустые клетки можно вставлять ничего не значащие буквы. Текст — только на английском языке.",
      inputHint:
        "Текст: только латиница (A–Z). Файл .txt или ввод с клавиатуры. Схема решётки фиксирована (см. матрицу выше).",
      language: "en" as TaskLanguage,
    },
    {
      id: "vigenere",
      title: "Задание 2",
      formulation:
        "Шифр Виженера с прогрессивным ключом: при повторном применении ключа символы ключа циклически сдвигаются на одну позицию в алфавите (например, MODE → NPEF → OQFG). Таблица Виженера: пересечение столбца буквы текста и строки буквы ключа даёт букву шифротекста. Текст — только на русском языке.",
      inputHint:
        "Текст: только кириллица (А–Я). Ключ: только буквы русского алфавита. Файл .txt или ввод с клавиатуры.",
      language: "ru" as TaskLanguage,
    },
  ] as Lab1Task[],
};
